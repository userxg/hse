1. Сортировка *Пузырьком*, or *insertion*, имеющая сложность в худшем случае O(n^2), может также выполнить задачу сортировки за `O(n)` время, если коллекция предварительно отсортирована.
2. 2. В теории чисел делитель числа может быть простым или составным. 
3. 3. Основной идеей *exponential* поиска, яв-ся поиск интервала, на котором может присутствовать целевой элемент. Этот алгоритм может быть особенно эффективен если целевой элемент находится в отсортированной коллекции.
4. Код на машинном языке может быть напрямую (без посредников) выполнен процессором.
5. 5. Временная сложность доступа к элементам массива константная достигается за счёт того, массив это непрерывный набор ячеек в памяти и если мы знаем адрес первого эл-та (то есть массива) мы можем прибавить к этому адресу номер нашего искомого.
6. Для того чтобы воспользоваться алгоритмами бинарного поиска и экспоненциального поиска, необходимо чтобы коллекция была отсортирована. В этом случае асимптотическая оценка времени данных поисков составит O(log(n)).
7.  Алгоритм Евклида решает задачу нахождения наибольшего общего делителя двух чисел. Также расширенный алгоритм Евклида return x,y where `a*x + b*y = gcd(a,b)`
8. В случае отсутствия элементов с заданными свойствами в неотсортированном массиве сложность их поиска составит 'O(n)'.
9. Символьные обозначения адресов в памяти, используемые в программах, называются **указателями**.
10. 